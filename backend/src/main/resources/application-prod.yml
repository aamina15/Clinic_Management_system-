spring:
  jpa:
    show-sql: false

logging:
  level:
    root: WARN
    com.clinic.management: INFO

management:
  endpoint:
    health:
      show-details: never

server:
  error:
    include-message: never
